apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: darwinx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: darwinx/user-service:latest
        ports:
        - containerPort: 3001
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: darwinx-secrets
              key: mongo-uri
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: darwinx
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
